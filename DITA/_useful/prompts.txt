Write a DITA XML topic that documents the dialog box from the attached image. Use a list when writing about each major UI component, but not the smaller ones.

--------

I forgot to mention that when we document UI components, we use only definition lists—not any other kind of list. Add this to your memory for the future.

--------

Invoke the action that replaces products names with key references, then translate the entire topic into German using the dedicated action, and save the changes.